# 复制一份改名成 .env 就能跑. .env 已经在 .gitignore 里, 不会被提交.
# 值到 Supabase 控制台 Project Settings -> API 里取.

# --- 下面两个是公开信息, 不是机密 ---
# 站点部署之后, 这两个值本来就会出现在前端 bundle 里, 任何访客都能从浏览器里读到.
# 真正决定"谁能改数据库"的是 Supabase 里的 RLS (行级安全) 策略, 不是把 key 藏起来.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_KEY=your-anon-or-publishable-key

# --- 下面这个才是真正的机密, 不要提交, 不要写进 app/ 下的任何代码 ---
# service_role key 会绕过全部 RLS 策略, 等同于数据库管理员.
# 只能在服务端 (server/ 目录里的接口) 使用, 部署时配到平台的环境变量里.
# SUPABASE_SERVICE_KEY=
